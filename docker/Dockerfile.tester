# DevilChain Automated Test Runner
FROM python:3.11-slim
WORKDIR /tests

RUN pip install --no-cache-dir \
    requests==2.31.0 \
    pytest==8.2.0 \
    httpx==0.27.0 \
    colorama==0.4.6

COPY docker/tests/ ./

CMD ["python", "-m", "pytest", ".", "-v", "--tb=short", "--color=yes"]
